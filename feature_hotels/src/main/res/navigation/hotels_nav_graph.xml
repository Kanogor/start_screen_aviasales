<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/hotels_nav_graph"
    app:startDestination="@id/hotelsFragment">

    <fragment
        android:id="@id/hotelsFragment"
        android:name="ru.kanogor.feature_hotels.presentation.HotelsFragment"
        android:label="@string/hotels"
        tools:layout="@layout/fragment_hotels">
        <deepLink
            android:id="@+id/deepLink"
            app:uri="android-app:/hotelsFrag" />
    </fragment>
    <deepLink
        android:id="@+id/deepLink2"
        app:uri="android-app:/hotelsFrag" />
</navigation>